---
"@universal-agents/cli": minor
---

- Introduce interactive `ua create skill` / `ua create rule` commands (powered by `@inquirer/prompts`) to scaffold empty `.agents` stubs without manual path wrangling.
