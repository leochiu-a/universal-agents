# Changesets

This directory is managed by `@changesets/cli`, which keeps track of release notes
for each pull request. Run `pnpm changeset` whenever you make a change that
should be included in the changelog.
